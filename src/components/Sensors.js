import React from 'react';
import { View, Text } from 'react-native';

const Sensors = () => {
  return (
    <View className="flex-1 items-center justify-center bg-gray-800">
      <Text className="text-blue-400 text-xl">Sensors</Text>
    </View>
  );
};

export default Sensors;
